import { MoonIcon, SunIcon } from "./Icons";
import { Badge } from "./Ui";

export function DashboardHeader({ styles, systemStatus, modeAuto, lastUpdate, isDarkMode, onToggleTheme, title = "Dashboard hệ thống cảnh báo cháy", subtitle = "Giám sát thời gian thực" }) {
  return <div className={`${styles.panel} ${styles.section}`}><div className="flex flex-col gap-4 xl:flex-row xl:items-center xl:justify-between"><div><p className="text-sm text-slate-500">{subtitle}</p><h2 className="text-2xl font-bold text-slate-900">{title}</h2></div><div className="flex flex-wrap items-center gap-3"><Badge tone={systemStatus.tone} className="px-4 py-2 text-sm">{systemStatus.label}</Badge><Badge className="px-4 py-2 text-sm">{modeAuto ? "AUTO" : "MANUAL"}</Badge><Badge className="px-4 py-2 text-sm">{lastUpdate}</Badge><button type="button" onClick={onToggleTheme} aria-pressed={isDarkMode} aria-label={isDarkMode ? "Chuyển sang giao diện sáng" : "Chuyển sang giao diện tối"} className="inline-flex items-center gap-2 rounded-2xl border border-slate-300 bg-white px-4 py-2 text-sm font-semibold text-slate-700 transition hover:-translate-y-0.5 hover:bg-slate-50 hover:shadow-sm focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-slate-900">{isDarkMode ? <MoonIcon className="h-4 w-4" /> : <SunIcon className="h-4 w-4" />}<span>{isDarkMode ? "Tối" : "Sáng"}</span></button></div></div></div>;
}

import { Button } from "./Ui";

export function ConnectionPanel({ styles, wsUrl, onWsUrlChange, wsState, onConnect, onDisconnect, sourceMode, packetCount, lastError }) {
  return <div className={`${styles.panel} ${styles.section}`}><div><h3 className="text-base font-semibold">Kết nối ESP8266 / ESP32 qua Wi-Fi</h3><p className="mt-1 text-sm text-slate-500">Dashboard nhận telemetry qua WebSocket. STM32 gửi UART sang ESP, ESP bridge dữ liệu ra Web.</p><div className="mt-4 grid grid-cols-1 gap-3 lg:grid-cols-[minmax(0,1fr)_auto_auto]"><input value={wsUrl} onChange={(event) => onWsUrlChange(event.target.value)} className={styles.input} placeholder="ws://192.168.4.1/ws" /><Button onClick={onConnect} disabled={wsState === "connecting"}>{wsState === "connected" ? "Kết nối lại" : wsState === "connecting" ? "Đang kết nối" : "Kết nối Live"}</Button><Button variant="outline" onClick={onDisconnect}>Ngắt / về mô phỏng</Button></div><div className="mt-3 flex flex-wrap gap-3 text-xs text-slate-500"><span>Packets: {packetCount}</span><span>Mode: {sourceMode === "live" ? "Live WebSocket" : "Simulation"}</span><span>Socket: {wsState}</span></div>{lastError ? <p className="mt-3 text-sm text-red-600">{lastError}</p> : null}</div></div>;
}
